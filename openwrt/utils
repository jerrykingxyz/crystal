#!/usr/bin/env bash

UTILS_DIR=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
. "$UTILS_DIR/../lib/config.sh"
. "$UTILS_DIR/../lib/render.sh"

kill_with_process_name() {
    local name=$1
    local pid=$(pgrep -f $name)
    if [ ! -z "$pid" ]; then
        kill -9 $pid
    fi
}
