#!/bin/sh

. "$OPENWRT_DIR/../lib/config.sh"
. "$OPENWRT_DIR/../lib/render.sh"

kill_with_process_name() {
    local name=$1
    local pid=$(pgrep -f $name)
    if [ ! -z "$pid" ]; then
        kill -9 $pid
    fi
}
